# Copie este arquivo pra `.env` e ajuste conforme o ambiente.
# Sem essa variável, o frontend cai no default de dev local (http://localhost:3001).

# URL do backend. Em produção, aponte pra URL pública do backend no Render,
# ex.: https://barberpro-api.onrender.com
VITE_API_URL=http://localhost:3001
